import styles from "../styles/header.module.css";

const Header = () => {
  return (
    <div className={styles.container}>
      <footer>Weather-Report</footer>
    </div>
  );
};

export default Header;
